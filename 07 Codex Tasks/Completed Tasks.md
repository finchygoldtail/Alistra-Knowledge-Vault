---
title: Completed Tasks
type: codex
status: active
owner: Alistair
created: 2026-08-02
updated: 2026-08-11
tags: [codex, completed]
---

# Completed Tasks

## Completed

- 2026-08-11: Completed and deployed an adversarial security remediation covering authentication, tenant authorization, Storage access, Firebase rules, browser headers and vulnerable dependencies. Commit `eeeab79` is live through Vercel and Firebase project `fibre-gis-v2`; all existing FRIDAY endpoints were preserved. Verification included production builds, 277 application/security tests, 13 component tests, 33 emulator rules tests, TypeScript checks, zero known production dependency vulnerabilities in both npm trees, Vercel success status, and a live asset/browser smoke check. See [[Security Remediation 2026-08-11]].
- 2026-08-07: Closed out Round 4 with the last two fixable findings — `stampUserSession`'s businessId validation gap and its silent `console.warn`-only failure. Re-traced `resolveStorageContext`'s platform-admin exemption logic (super_admin is the only role that switches business context, and it's already exempted from the same-company check that gate enforces) and confirmed routing `stampUserSession` through it doesn't actually risk the flow the first pass was cautious about — kept the `snapshot.exists` guard as a second check regardless. Added one automatic retry plus a louder `console.error` on repeated failure, still non-blocking by design. 10 of 19 Round 4 findings fixed; the rest are accepted architecture, need a real design decision (rate limiting), or aren't code bugs. See [[Active Bugs]] — this round is closed.
- 2026-08-07: Worked through the remaining Round 4 audit findings — 6 more fixed (4 Medium, 2 Low), bringing the round's total to 8 of 19 fixed. See [[Active Bugs]] for the full breakdown.
	- Fixed: `createdByUid`/`createdAt` spoofable on edits across employees/plant equipment/vehicles/crews — new `resolveCreationFields()` in `storageAccess.ts` reads the real stored value server-side on an edit instead of trusting client-supplied fields.
	- Fixed: QR scanner camera restarting on every scan attempt — reused the existing, already-tested `useStableCallback` hook (built earlier for the identical problem in `JointMapCanvas.tsx`) rather than hand-rolling a new ref pattern.
	- Fixed: User Register Inspector discarding unsaved edits on unrelated saves — the sync effect now keys off the selected user's uid (a stable primitive) instead of the selected-user object (a new reference on every list refresh).
	- Fixed: Fleet tab's `Promise.all` hiding which of two data sources failed — switched to `Promise.allSettled`, error now names the failing register and doesn't discard data that loaded fine from the other.
	- Fixed: `listCompanyCrews` writing an unnecessary audit-log entry on every read, and `CrewPicker` re-fetching uncached from four different drawers — removed the audit write, added a shared 60s-TTL module-level cache to `CrewPicker.tsx`.
	- Fixed: removed the unused `packs` Firestore composite index (`lifecycleStatus`+`plannedDate`, no query used it).
	- Fixed: `VehicleQrLabel`/`PlantEquipmentQrLabel` both conflated "still generating" with "failed" on a QR-encoding error — added a distinct failure state to both (same bug, same copy-paste origin).
	- Deliberately left open: `stampUserSession`'s businessId validation gap — the audit-recommended fix (routing through the strict membership check) risks breaking the legitimate super-admin cross-company session flow, unverifiable without a live environment, not worth the risk for a finding already rated low-impact. Also left: rate limiting (needs a real design decision, not a quick fix), the cross-business read-access architecture (accepted since 2026-08-04), and everything in the audit that wasn't a genuine code bug (the `any`-typing tracking methodology, the thin-CRUD-wrapper convention, the 3,400-line `JointMapManager.tsx` decomposition).
	- Tests: full suite re-run after every fix — 178/178 passing throughout, typecheck/functions-build/frontend-build all clean.
- 2026-08-07: Fixed the two Round-4-audit findings recommended as immediate next actions (the round's only High, and the one Medium with a real exploit angle) — see [[Active Bugs]].
	- Fixed: the three bulk-import panels (Employees, Plant Equipment, Vehicles) each made one Cloud Function call per row instead of a batch — added `bulkUpsertCompanyEmployees`/`bulkUpsertCompanyPlantEquipment`/`bulkUpsertCompanyVehicles`, each committing an entire import (up to 500 rows) in a single Firestore batched write instead of N sequential round trips. Bulk-imported employee credentials no longer need a separate per-credential call either — they're embedded directly in the same batched write, since a bulk import is always creating a new record, never merging into one with existing credentials.
	- Fixed: `recordCompanyDocument` had no role gate and no validation that a client-supplied `downloadUrl`/`storagePath` actually pointed at the caller's own company's Storage tree — a "user"-role account could tag an external phishing URL as a trusted compliance document. Deliberately did NOT add a role gate (self-service document uploads by field users, e.g. attaching an inspection photo, are intended) — instead validated the URL/path against the caller's own company prefix, mirroring the existing ticket-attachment validator (`functions/src/index.ts`'s `isValidTicketAttachmentUrl`). Extracted the new validators into a dependency-free `storageUrlValidation.ts` (same pattern as `sessionTokenComparison.ts` from the 2026-08-07 batch) after discovering `storageAccess.ts` itself can't be imported under the plain-node test harness. Also added server-side `category` validation against the known category union while in the same function.
	- Tests: 1 new suite (`storage-url-validation.test.ts`, 12 cases covering the phishing-URL and cross-company scenarios) — 178/178 passing.
- 2026-08-07: Moved Plant Equipment, Vehicles and Employees into the Project Workspace's Management tab group. Plant Equipment/Vehicles were re-filed from the "Field" tab group; Employees was fully removed from the Manage Users admin panel (was a section there) and became a genuine Project Workspace tab instead — no duplicate entry point left behind. See [[Manage Users]].
- 2026-08-07: Round-4 reliability/cost/security audit of `fibre-gis` — re-verified every item still open from the 2026-08-06 baseline plus a dedicated review of everything shipped in the two 2026-08-06/07 feature batches (report: [fibre-gis Audit — Round 4](https://claude.ai/code/artifact/324913d5-bfae-41d3-9094-57757b60385b)). Result: 0 Critical, 1 High, 10 Medium, 8 Low. See [[Active Bugs]] for the full list. Notable: `recordCompanyDocument` has no role gate at all (any "user"-role account can attach an unvalidated URL to a compliance record); the three new bulk-import panels do one Cloud Function call per row instead of a batched write (the round's only High); and the `any`-typing tracking figure across audit rounds turned out to be using inconsistent grep methodology, not a real reduction — flagged for whoever maintains that metric. None of these are fixed yet — audit-only pass, matching Rounds 2 and 3.
- 2026-08-07: Built and deployed seven follow-up features requested after a day of using the 2026-08-06 build: a Supervisor role, single-session login enforcement, an employee credential/accreditation roster, vehicle QR-scan checkout, a Crews Client Modules toggle, a Management "Fleet" tab, and an as-built document generator. Pushed to `main`, Cloud Functions/Firestore rules/indexes deployed live to `fibre-gis-v2`. See [[Decision Log]] for the design calls and [[Platform Scope]] for the updated scope.
	- Added: Supervisor role (approve work-pack readiness, sign off completion, resolve QA flags/snags) — see [[Role Matrix]], [[Concurrent Login Control]]. A mandated `grep -rn '=== "user"'` sweep before merging caught ~20 bare role checks across `storageCallables.ts`, `storageHttpApi.ts` and `documentCallables.ts` that would otherwise have silently let Supervisors through Manager-only actions (map-asset edits, work-pack CRUD, FAS import, document deletion) the moment the role existed — all reclassified to the correct gate before shipping, not found by the user.
	- Added: single-session enforcement — see [[Concurrent Login Control]].
	- Added: employee credential/accreditation roster with renewal dates and an Excel bulk-upload template — see [[Employee Credentials]].
	- Added: vehicle QR-scan checkout, matching Plant & Equipment's existing scan flow — see [[Vehicle Management]].
	- Added: Crews register now toggleable in the Client Modules admin screen, alongside Plant Equipment and Vehicles — see [[Manage Users]].
	- Added: Management dashboard "Fleet" tab — vehicles and what plant/equipment is currently stored in each one.
	- Added: as-built document generator — manual "Generate As-Built" button per project/area, own workspace tab, reuses the existing Job Pack map-paginator — see [[As-Built Generation]].
	- Fixed (found in the mandated post-implementation code review, before the user saw it): the session-enforcement Firestore listener read its local comparison token once at mount and bailed out permanently if empty — but the token-minting callable resolves asynchronously, so on a fresh sign-in the listener almost always lost that race and never subscribed at all. That silently defeated the entire single-session feature for the most common case (a device that had just signed in never got kicked when a second device signed in after it). Fixed by reading the token fresh on every Firestore snapshot instead of once at mount.
	- Fixed (reported by the user immediately after, same day): the map's Layers sidebar listed every asset-type group regardless of Client Modules entitlements — a disabled module's layer just sat there permanently unchecked instead of disappearing. Extended the existing `applyMapAssetModuleEntitlements` gating (which already correctly hid the assets themselves) to also filter the sidebar's group list — see [[Main Map]].
	- Fixed (reported by the user, same day): the User Register list in Manage Users had no height cap or scroll of its own, forcing a scroll of the whole admin page to reach users further down the list. Worse, a `useEffect` always force-reselected the first user the instant selection was empty, so there was no possible "nothing selected" state — clicking the already-active row did nothing visible, and there was no way back to a neutral list view. Gave the table its own capped, independently-scrolling viewport with a sticky header, and made clicking the active row toggle a real deselect. See [[Manage Users]].
	- Fixed (reported by the user immediately after, same day): the "Active Users" tile in the admin summary strip looked identical to its clickable siblings (Licence, Seats, Enabled Modules, Employees, Storage, Region — all of which jump to their section) but had no `onClick` at all. Wired it to jump to the User Register, matching the rest of the strip.
	- Tests: 4 new suites (role gates, session-token comparison, employees module, as-built adapter) plus 2 existing suites updated for the new module/owner-type registries — 168/168 passing. Firestore rules tests for the new `employees` collection remain blocked by the same missing-Java-for-the-emulator constraint noted 2026-08-06.
- 2026-08-06: Built and deployed Plant & Equipment, Vehicle Management, a shared Crew register, and a Smart Work Pack upgrade — the first genuinely new production modules added to the platform (previously unscoped, confirmed via full-repo search before starting; see [[Platform Scope]]). Pushed to `main`, Cloud Functions/Firestore rules/indexes deployed live to `fibre-gis-v2`.
	- Added: company-wide `plantEquipment` and `vehicles` Firestore registers (not per-area), each with transactional check-in/out or handover (race-safe via `firestore.runTransaction`), fault/defect reporting, service/inspection/calibration tracking, a compliance alert dashboard, CSV export, and a "Download Bulk Add Template" Excel workflow matching the app's existing template-download convention (AG joints, street cabs, address sheets). See [[Plant and Equipment]], [[Vehicle Management]].
	- Added: QR-code label printing (reused the existing `qrcode` dependency) and camera-scan check-out on Plant & Equipment, with manual asset-tag entry as the fallback where the browser's `BarcodeDetector` API isn't supported (notably iOS Safari).
	- Added: shared `crews` register and a generic `documents` metadata collection (certificates, MOT/insurance, RAMS, permits, drawings, evidence) reused by all three new modules and by the Work Pack upgrade, extending the existing `uploadBusinessAssetFile` Storage path rather than adding a new one.
	- Changed: Plant/Vehicle check-out, handover and routine inspections were initially built manager-only, then reopened to any signed-in user the same day — see [[Decision Log]] for why.
	- Fixed: `recordCompanyVehicleHandover` never updated the vehicle's `status`, so a vehicle stayed "Available" in the register after being taken. Found while relaxing the permission model above.
	- Fixed: the generic Production KPI header strip was leaking onto the two new tabs — see [[Decision Log]].
	- Extended the existing Work Pack additively (`lifecycleStatus`, crew/plant/vehicle/material requirements, RAMS/permits/drawings, a non-blocking readiness-warning check, snags) without touching the existing `WorkPackStatus` field or the live production-logging chain it drives — see [[Work Packs]] and [[Decision Log]].
	- Tests: 5 new suites (crews, plant equipment, vehicles, document types, work-pack readiness — 12 cases covering every missing-requirement warning) — 151/151 passing. Firestore rules tests were planned but not run — the emulator needs Java, unavailable in the build environment; flagged as an open follow-up.
- 2026-08-05: Round-3 reliability/cost/security re-audit of `fibre-gis` — re-verified every 2026-08-04 finding against current code, plus a dedicated security review of the new ticketing system and an adversarial review of today's five workspace-page changes (report: [Reliability, Cost & Security Audit — Round 3](https://claude.ai/code/artifact/cc2825c7-eec9-42f1-ace4-061f87b1432a)). Result: 0 Critical, 6 High, 9 Medium, 6 Low, 8 items resolved since the last pass. See [[Active Bugs]] for the current list — notably two High-severity bugs were found in today's own work (Assets' DP Capacity Risk tab reads the wrong asset list; Reports' top tabs don't actually switch the displayed report) and a cross-tenant admin-escalation path plus an unvalidated-attachment-URL issue were found in the new ticketing system. None of these four are fixed yet.
- 2026-08-05: Wired up the dead/disabled tabs and buttons circled across the Permit Register, Assets, QA/Walk-off, Reports and Management workspace pages, plus built the internal ticketing/helpdesk system. Pushed to `main` on `finchygoldtail/AlistraGIS` in `fibre-gis`, Cloud Functions and Firestore rules deployed live. See [[Decision Log]] for the architecture calls this raised and [[Active Bugs]] for what's still open (Street Manager applications/notices integration, general asset bulk-import, and QA Walk-Off full audit wiring all remain deferred — see below).
	- Fixed: `WorkspacePermits.tsx`, `WorkspaceAssets.tsx` and `WorkspaceQA.tsx` had tab bars with no `onClick`/state at all — only the first tab ever rendered, regardless of which was clicked. Wired real tab state into all three, reusing existing logic per tab rather than building new (`AuditHistoryPanel` for History/Evidence, `getWorkspaceDpCapacityRisk`/`getOperationalDpStatus` for Assets' Health/Lifecycle tabs, the file's own already-computed QA/Readiness data for QA's tabs).
	- Fixed: `WorkspaceReports.tsx`'s top tab bar (Production/QA & Audits/Permits/Close-out) duplicated what its own "Report Library" sidebar already did — made the top tabs and the sidebar selection the same synchronized state instead of building four redundant copies of the library+preview layout. Added a real "Templates" tab reusing the existing template-download functions.
	- Fixed: `WorkspaceManagementDashboard.tsx`'s "Open Weekly Report"/"Export Snapshot" buttons were permanently disabled placeholders — wired both to real CSV exports using the app's existing generic `exportToCSV` utility (`src/services/csvExport.ts`) and the management page's own already-imported data-building functions.
	- Fixed (found while testing, unrelated to the above): `JointMapManager.tsx` referenced `handleNormalAssetCreated` ~1,000 lines before its own declaration, throwing a `ReferenceError` on every render and crashing the entire Map. Traced all its dependencies and moved the block to where they're satisfied; verified the 15 ESLint `react-hooks/refs` findings this unmasked are pre-existing, safe patterns (a ref that's never reassigned after mount, and a factory function that only reads its refs inside a later callback), not new bugs.
	- Fixed (found while testing, unrelated to the above): `workspaceMetrics.ts`'s `buildHeaderMetricItems` called an undefined `formatNumberFn` instead of the already-exported `formatNumber` in the same file — a plain typo crashing `ProjectWorkspace.tsx` outright and blocking every workspace tab, not just the ones above.
	- Added: internal ticketing/helpdesk system — `businesses/{businessId}/tickets` + `ticketEvents` Firestore collections, written only through 7 new Cloud Functions callables that reuse the existing `assertCompanyMember` membership/role check (no new auth logic). Frontend in `src/features/tickets/`, mirroring the existing `src/features/friday/` module: "Submit a Request" for any signed-in user, admin-gated "Support Tickets" queue, both wired into `UserMenu.tsx`. Comment attachments reuse `AuditPhotoUploader` unchanged.
- 2026-08-02: Created initial AlistraGIS Obsidian vault structure.
- 2026-08-04: Round-2 reliability/cost/security audit of `fibre-gis`, followed by a fix pass on the 2 Critical and 7 High findings (report: [Reliability, cost & security audit — Round 2](https://claude.ai/code/artifact/4eb9344d-8be3-4a04-9c47-9b31f275de3c)). Pushed to `main` on `finchygoldtail/AlistraGIS`. See [[Active Bugs]] for what's still open and [[Decision Log]] for the two decisions this raised.
	- Fixed: cable-draw mousemove handler scanned every map asset per raw pointer event with no throttling, causing visible lag while drawing on tablets. Now coalesced to one lookup per animation frame.
	- Fixed: field photo capture only attached uploaded photo URLs to an asset after the whole batch succeeded — a dropped connection mid-batch silently lost already-uploaded photos. Now persists per-photo as each upload completes, with retry for failures. See [[Field User Experience]], [[Offline Mode]].
	- Fixed: every asset save double-wrote the audit trail to two Firestore collections (`assetChangeLogs` + `assetActivityLogs`); the second had no reader anywhere in the app. Now writes once. See [[Audit Logging]].
	- Fixed: four save-failure paths told the user a failed save was "safely backed up locally" without checking whether the local backup actually succeeded.
	- Fixed (partial): added `vitest` + Testing Library to the repo (previously no component/rendering test coverage existed) and a `useStableCallback` hook, with a regression test, so the 17 callback props the map canvas passes into the marker/cable-line layers have a stable identity. Does not yet memoize `JointMapManager`'s ~800-property render-context object — see [[Active Bugs]].
	- Drafted, not deployed: `firestore.rules`/`storage.rules` changes locking the `assetChangeLogs` collection to Cloud-Function-only access and adding a size/type allowlist to the `asset-uploads` Storage path. Needs review before deploy.
